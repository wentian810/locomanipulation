#!/usr/bin/env bash
# Copy to configs/secrets.env (gitignored), fill with newly rotated credentials,
# then run: source configs/secrets.env

export TENCENTCLOUD_SECRET_ID="REPLACE_WITH_ROTATED_SECRET_ID"
export TENCENTCLOUD_SECRET_KEY="REPLACE_WITH_ROTATED_SECRET_KEY"
